# Stable rustfmt settings
max_width = 100
hard_tabs = false
newline_style = "Unix"

# Import formatting (stable)
reorder_imports = true

# Other useful stable options
use_small_heuristics = "Max"
merge_derives = true
use_field_init_shorthand = true